services:
  - type: web
    name: cursorf1
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 22.14.0
      - key: SKETCHFAB_TOKEN
        sync: false
